# Notes

This repository contains numerical methods for interpolation, splines, and integration.

- `data/` holds sample CSV files.
- `results/` contains output directories for figures and tables.
- `tests/` contains pytest-based unit tests.
